# Getting Started
This sections explains how to plug in your own architectures into this codebase

---
- **`core/`** → main functionality organized into modular subcomponents  
- **`java/`** → handles integration with external JVM-based services  
- **`orchestrator.py`** → central controller that ties everything together  


1. **Install dependencies**
   `pip install -r requirements.txt`

2. 